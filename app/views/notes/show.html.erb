<h1>Note by <%=@note.user.username%></h1>
<h5>"<%= @note.content %>"</h5>
<%="Created  #{time_ago_in_words(@note.created_at)} ago."%>
<h5>Tags:</h5>
<% @note.tags.each do |k| %>
	<%= k.name %>
<% end %>
| 
<%= link_to "edit", edit_note_path(@note)%>